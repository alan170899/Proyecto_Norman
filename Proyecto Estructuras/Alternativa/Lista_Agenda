#include <stdlib.h>
#include <stdio.h>

struct Lista {
	struct *contacto;
	contacto *siguiente;
	contacto *anterior;
}

struct Contacto {
	char *nombre;
	char *telefono;
}

void crear_contacto(struct Lista *Agenda, char NuevoNombre, char NuevoNumero);




int main (){

	struct Lista Agenda = NULL;
	
	crear_contacto(&Agenda,   

return 0;
}

void crear_contacto(struct Lista *Agenda, char NuevoNombre, char NuevoNumero){
	if (Agenda == NULL){
		Agenda = (struct Lista*) malloc (sizeof (strcut Lista));
		Contacto = (struct Contacto*) malloc (sizeof (struct Contacto));

	
	contacto->nombre = NuevoNombre;
	contacto->telefono = NuevoNumero;

}








 

